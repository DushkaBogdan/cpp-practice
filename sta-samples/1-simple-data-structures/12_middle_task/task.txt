The disk file, the path to which is passed as the first command-line argument, contains records about people, 1 line per person, in the following format:

<surname> <age>

The second argument on the command line is a number representing the cutoff age.
Display the last names and ages of all people who are over the age limit.
Try to handle all possible errors such as non-existent file, bad format or age.
